# Agent Rules

## Always Push Changes
Whenever you complete a task and have made modifications to code or files, you MUST automatically run `git add`, `git commit`, and `git push` before returning control to the user. Always push changes to GitHub.
